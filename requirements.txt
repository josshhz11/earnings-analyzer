# PDF parsing — pymupdf chosen over pdfplumber; see DECISIONS.md for reasoning.
pymupdf>=1.24.0

# Anthropic SDK — LLM calls for extraction/eval/revision/summary stages.
# >=0.121.0 because src/extraction/claim_extractor.py uses client.messages.parse()
# + output_format (structured outputs), a newer SDK feature.
anthropic>=0.121.0

# Structured-output schemas passed to client.messages.parse() (claim_extractor.py).
# Also a transitive dependency of anthropic itself, but declared directly since
# our own code imports it.
pydantic>=2.0.0

# Testing
pytest>=8.0.0

# Environment variable loading (.env -> ANTHROPIC_API_KEY etc.)
python-dotenv>=1.0.0
